import { defineConfig } from 'vite'

// When deploying to GitHub Pages under a repository site, set base to
// '/<repo-name>/' so assets and script tags point to the correct location.
export default defineConfig({
  base: '/EZ-Labs-Frontend-Intern-Assignment/'
})
